##############################################################################
################################ makefile-s ##################################
##############################################################################
#                                                                            #
#   makefile of TemplateBlock and its dependencies, excluding SMS++          #
#                                                                            #
#   The makefile defines internally all external libraries required by       #
#   TemplateBlock, excluding the SMS++ core ones whose symbols are           #
#   assumed to have been defined already (typically by including             #
#   SMS++/lib/makefile). Accordingly, all necessary OBJ, H, -I directives,   #
#   external libraries + -L< libdirs > are added to the corresponding        #
#   TmpBk* macros.                                                           #
#                                                                            #
#   Input:  $(CC)        = compiler command                                  #
#           $(SW)        = compiler options                                  #
#           $(SMS++INC)  = the -I$( core SMS++ directory )                   #
#           $(SMS++OBJ)  = the core SMS++ library                            #
#           $(TmpBkSDR)  = the directory where TemplateBlock source is       #
#           $(TmpBkODR)  = the directory where the objects have to be put    #
#                                                                            #
#   Output: $(TmpBkOBJ)  = the final object(s) / library                     #
#           $(TmpBkLIB)  = external libraries + -L< libdirs >                #
#           $(TmpBkH)    = the .h files to include for TemplateBlock         #
#           $(TmpBkINC)  = the -I$( TemplateBlock directory )                #
#                                                                            #
#                             Antonio Frangioni                              #
#                         Dipartimento di Informatica                        #
#                             Universita' di Pisa                            #
#                                                                            #
##############################################################################

# define & include the necessary modules- - - - - - - - - - - - - - - - - - -
# if a module is not used in the current configuration, just comment out the
# corresponding include line
# each module outputs some macros to be used here:
# *OBJ is the final object/library
# *H   is the list of all include files
# *INC is the -I< include directories >

# include the makefile requiring all external modules in input
include $(TmpBkSDR)/makefile

# macros to be exported - - - - - - - - - - - - - - - - - - - - - - - - - - -

# append external -I directives to TmpBkINC, TmpBkOBJ and TmpBkH as defined
# by the makefile, thus the := assignment has to be used (use GNU make)
TmpBkOBJ := $(TmpBkOBJ)
TmpBkINC := $(TmpBkINC)

# external libraries for TemplateBlock
TmpBkLIB =

############################ End of makefile #################################
